# DESIGN.md — Ku-nya 新标签页（方向 B · 暗房）

> 本文件是唯一事实源。改风格 = 先改这里。
> 选定记录：`design-previews/2026-09-15-kunya-ntp-search-modes/selection.json`
> 方向 B「暗房」· 视觉冒险度 7 / 动效强度 6 / 信息密度 5（用户对话确认）
> 改版记录：2026-09-15 图墙由瀑布流改为**精确拼合拼图墙**（Linux 式空间分割 × Apple 材质），见 §4 拼块、§5 布局
> 改版记录：2026-09-19 搜索升格为**聚焦升起**（Spotlight 式夺权，方向 A 选定记录：`design-previews/2026-09-19-kunya-search-primary/selection.json`），见 §4 胶囊、§9 Motion
> 改版记录：2026-09-19 设置 popup 重设计为**暗房延伸**（方向 A 选定记录：`design-previews/2026-09-19-kunya-popup-settings/selection.json`，拨盘 4/4/6），见 §4 设置面板；同批落地 ⇧R 换一批与 AI / 收藏下限 / 屏蔽画师三重过滤
> 改版记录：2026-09-20 **Chrome 书签联动**（拼块收藏星标 + 搜索栏书签联想），见 §4 拼块/胶囊、§6 深度层级
> 改版记录：2026-09-20 **首页书签栏**：胶囊上方一排 Chrome 书签栏镜像，实时同步、点击直达，见 §4 书签栏

## 0. 功能契约（高于一切美学）

新标签页打开后，3 秒内必须成立：

1. **一眼是画**：满屏插画（pixiv 日榜随机）立即铺开，是页面的主角
2. **一眼找到搜索**：底部中央的搜索胶囊是唯一常驻浮起控件，`/` 或 `Ctrl+K` 直接聚焦，任意可打印字符直接开始输入；**聚焦瞬间胶囊升到视口 38% 高度、图墙压暗让位**（Spotlight 式夺权），失焦/Esc 落回底部
3. **一次切换进入防误触**：胶囊右端开关在「纯看 / 交互」间切换，状态立刻可辨并记住
4. **回车即搜网页**：用 Chrome 默认搜索引擎搜索（等同传统地址栏搜索）；输入像网址则直接跳转

**插画是装饰（画廊），不是搜索对象。** 搜索栏搜的是网页，不是 pixiv。

## 1. 视觉主题与氛围

一间暗房：**只有画是亮的**。画布是暖调近黑，界面元素退到几乎消失，唯一的"材质"是插画本身和一层薄薄的暗角。

- 氛围层：整页叠一层暖色径向暗角（`opacity < 0.06`），模拟暗房边缘光衰，不抢画
- 不做发光、不做霓虹、不做渐变文字；对比全部由插画与暗底产生

## 2. 色板与角色

| 角色 | 值 | 用途 |
| --- | --- | --- |
| 画布 Void | `#131110` | 页面底色（暖调近黑，**不是纯黑**） |
| 表面 Surface | `rgba(19, 17, 16, 0.86)` | 胶囊底（**烟熏玻璃，不透明度过半**：白色文字必须压在够暗的底上） |
| 描边 Line | `rgba(255, 255, 255, 0.10)` | 胶囊与开关描边 |
| 描边弱 Line-soft | `rgba(255, 255, 255, 0.06)` | 分隔线 |
| 主文字 Ink | `rgba(255, 255, 255, 0.92)` | 输入文字、模式标签 |
| 次文字 Ink-soft | `rgba(255, 255, 255, 0.68)` | placeholder、提示 |
| 重音 Accent | `#E0A45E`（暖琥珀） | **只用于交互**：焦点环、开关开启态、::selection |

纪律（偷自 Apple）：**全页只有一个彩色**，且只出现在交互状态里；非交互元素一律白/灰阶。

## 3. 排版规则

- 西文/数字：`"Segoe UI Variable Text", "Segoe UI", system-ui, sans-serif`，暗底上**正字距 +0.3px**（偷自 Raycast：暗色界面用正字距换通透感）
- 中文：`"PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`；行高 ≥ 1.5
- 微标签（`GALLERY` / `WEB SEARCH` 之类 10px 大写）：字距 1px（偷自 SpaceX 的航空刻字语气）
- 字重只用 400 / 500 / 600；**禁止斜体**；标题不做渐变
- 数字一律 `font-variant-numeric: tabular-nums`

## 4. 组件样式

**书签栏**（2026-09-20，首页快捷访问）
- 搜索胶囊正上方（距底 88px，与胶囊同属底部安全带）一排小胶囊项：14px favicon（MV3 `_favicon` 端点，不外联）+ 标题（11.5px、148px 截断），内容是 **Chrome 书签栏的直接子项**（跳过文件夹），保持用户在 Chrome 里的排列顺序
- **实时同步**：不持任何自有数据——挂载时读一次，`chrome.bookmarks.onChanged` 触发即重读（实测部分环境不投递该事件），并兜底在标签页**重获焦点 / 可见**时重读；在 Chrome 书签管理器里整理完切回来，书签栏即时跟随
- 幽灵态与胶囊同拍（纯看静止 2s 淡到 16% + 下移 6px）；胶囊升起时它位于暗场之下（z 2 < 3），随图墙一起压暗并 `pointer-events: none`——搜索夺权时书签让位
- 单行不折行，溢出横向滚动（隐藏滚动条 + 两侧 20px 渐隐遮罩）；**一条共享的烟熏玻璃托架**（`rgba(19,17,16,.45)` + blur 14px，比胶囊的 0.86 安静一档）——裸字压在繁忙图墙上过不了暗房自己的可读性纪律（P-5 同因）；条项 hover 以 10% 白底提亮
- 空书签栏 = 不渲染，不展示空壳；popup「书签」卡片可整体关掉

**搜索胶囊**（页面唯一常驻组件）
- 形状：999px 全圆角；静止时宽 `min(560px, 62vw)`、高 48px
- 材质：`backdrop-filter: blur(18px) saturate(120%)` + 表面色 + 1px 描边。表面色取 0.86 不透明度：插画里有大片白（漫画页），清玻璃会让胶囊变亮、白字失去对比度，可读性优先于通透感（P-5）
- 深度：多层染色阴影（偷自 Raycast 的 inset-highlight 栈）
  `inset 0 1px 0 rgba(255,255,255,.10), 0 1px 2px rgba(0,0,0,.35), 0 8px 24px rgba(0,0,0,.45)`
- 左侧搜索图标 16px，次要色；右侧依次是**搜索按钮**与模式开关
- 聚焦态（升起）：胶囊中心落在视口 38% 高度（`bottom: calc(62dvh - 28px)`），加宽至 `min(640px, 68vw)`、加高至 56px；描边转 Accent 40% + 外圈 `2px` Accent 焦点环（offset 2px）；胶囊正上方浮出微标签 `WEB SEARCH`（10px 大写、字距 1px、延迟 60ms 淡入），正下方浮出提示行 `↵ 搜索 · ⇧R 换一批 · esc 收起`（11px、延迟 80ms 淡入），两者随胶囊同行、`pointer-events: none`

**搜索按钮**（2026-09-19 用户增补）
- 输入框与开关之间的 30px 圆形按钮，箭头图标；静止时透明底 + 次要色图标，hover 提亮
- 输入框有内容时变为 Accent 底 + 深字——全页唯一彩色物件，且只为「执行搜索」这个动作点亮
- `mousedown` 阻止默认失焦：点击按钮时胶囊不坠落，直接执行搜索

**搜索暗场（scrim）**
- 胶囊升起时整页罩一层暖调径向暗场（`radial-gradient(ellipse at 50% 38%, rgba(19,17,16,.30), rgba(19,17,16,.62) 78%)`），中心最浅——刚好是胶囊升起的位置；`z-index: 3`（胶囊之下、图墙之上）、`pointer-events: none`，只压画不挡手
- 暗场是聚焦的临时状态（同字幕条的性质），不算第三层常驻浮层

**模式开关**
- 真 `<button role="switch">`，56×28px 轨道 + 22px 滑块，`aria-checked` 随状态
- 开启（交互）= 滑块右移 + Accent 底；关闭（纯看）= 滑块左移 + 中性灰
- 左侧文字标签随状态切换：`纯看` / `交互`（10.5px，正字距）

**拼块（插画）**
- 图墙不再是瀑布流：每幅插画是一块**精确拼合的拼块**——排版算法（`src/lib/tiling.ts`，见 §5）给每张图分一个宽高比等于原图的矩形，**零裁切**；拼块之间留 10px 均匀的灰缝（画布底色做勾缝），整墙读起来是一块带缝拼图
- 拼块皮肤（偷 macOS 窗口边缘语言）：10px 连续圆角 + 0.5px 内嵌毛发描边 + 顶部 1px 内高光；不是卡片，没有投影（投影只出现在交互态 hover）
- 默认 `filter: brightness(.94)`：把画压暗一档，让近黑的"房间"在拼块之间读得出来；交互模式 hover 时回到 `1.05` 并微增饱和（`saturate(1.06)`）
- 纯看模式：无链接、无 hover 变化、无字幕条，`cursor: default`
- 交互模式：hover / `focus-within` 时拼块上浮 4px + `scale(1.015)`（带轻微回弹的 `--ease-spring`）+ 柔和投影 + 玻璃描边提亮，图片缓推近 `scale(1.045)`（凑近看一幅挂画的感觉），底部滑出一条毛玻璃字幕条（作者 / 标题，10.5–11px）；点击作品页新标签打开
- **收藏星标**（2026-09-20）：交互模式下拼块右上角一枚 30px 毛玻璃圆形按钮（与字幕条同材质），hover / `focus-within` 才现身；点击把作品页（`pixiv.net/artworks/<id>`，标题为「标题 / 画师」）存入 Chrome 书签的 **Ku-nya 文件夹**（位于「其他书签」，查找而非死记 id，用户可在书签管理器里自由移动），再点即移除（按 URL 全树匹配删除）。已收藏的拼块星标**常亮琥珀实心**——这不是角标装饰，是交互状态标记；星标与链接是并列兄弟（链接充满内容盒），不是按钮套链接

**书签联想面板**（2026-09-20，胶囊的升起态组件）
- 输入时（此时胶囊必然处于升起态）防抖 160ms 查 `chrome.bookmarks`，胶囊下方滑出最多 5 条匹配：星图标 + 标题 + 域名，行高 8+12 padding，圆角 10px；选中行 8% 白底、图标点亮琥珀
- **键盘契约**：`↑↓` 移动选择；未选中任何行时 ↵ 仍是网页搜索（功能契约第 4 条不可让位），选中后 ↵ 当前标签打开该书签；`Esc` 照旧清空收起
- 提示行随面板切换文案：`↑↓ 选书签 · ↵ 打开所选 · esc 收起`
- 面板材质 = 胶囊材质（同 `--surface` + 同款阴影栈），位置 `top: calc(100% + 34px)` 给提示行留位；它是升起态的临时组件，随失焦消失，不算第三层常驻浮层

**设置面板（popup，方向 A · 暗房延伸）**

popup 是画廊的**调光台**：同一间暗房的材质，不是另一个产品。

- 画布：`--void` 底 + 顶部一抹琥珀余温（radial-gradient ≤5%）；宽 400px，超出纵向滚动
- **分组卡片 = 底片片夹**：6 张卡片（观看 / 书签 / 内容源 / 账户 / 过滤 / 屏蔽），圆角 10px + 1px 描边 + `rgba(255,255,255,.028)` 面；每张卡片上沿中央一枚 28×3px 的**片夹缺口**——分组的记忆点
- 行：标题左置（13.5px / 550）+ 一行 11.5px 次文字注释，控件右置；行间 1px 弱分隔线；宽控件（chips、七段分段）整行堆叠
- 控件语言：**琥珀只给开态的开关、焦点环、登录呼吸点**——分段控件选中态是「点亮」（白 12%）不是琥珀；过滤用中性方勾选，只有「内容安全」这种守护型开关用琥珀 switch
- chips：8% 白底 + 描边胶囊，`×` 按钮 hover 才亮起；输入行聚焦给琥珀焦点环
- 登录行：已登录 = 琥珀呼吸点（2.6s 透明度脉动，reduced-motion 静止）+ 用户名；未登录 = 灰点 + 「去 pixiv 登录」琥珀描边主按钮
- 「扩展已更新请重载」警告：琥珀描边 + 琥珀字，不用红色——它不是错误，是维护提示

## 5. 布局原则

- 图墙：**精确拼合的二叉空间分割**（exact-fit BSP，借 i3/bspwm 的分裂逻辑）。视口矩形递归二分裂，每片叶子放一张图；每个节点携带"自然宽高比"（叶子 = 原图宽高比；左右并排 = 子宽高比相加；上下堆叠 = 调和相加），建树时按面积均衡 + beam search 选切割方案，使整墙自然宽高比尽量贴近屏幕
- **零裁切纪律**：整墙按根节点的形状律 `w = aspect·h + beta` 绘制并居中（beta 携带灰缝的宽度贡献，因此拼块内嵌内容区与原图宽高比**严格相等**，伸缩为 0）——图拼不出屏幕形状时，余量成为对称的外框边距，永远不落在图上
- **图源纪律**：榜单接口给的 `_square` / `_custom` 缩略图是 CDN 中心裁切的方图，像素与 `illust_width/height` 不符；`api.ts` 一律归一化为等比 master 图，否则方图会被拼块拉伸
- **内容源**：图墙内容源 = 内置榜单（illust / manga / original / ugoira / newer / popular）、**算法推荐（discovery）**或**用户自定义标签分类**——popup 里维护标签列表，每个标签在内容源下拉里成为一个 `tag:<关键词>` 选项；标签源走 pixiv 搜索接口（`ajax/search/artworks`，按最新排序），条目自带宽高，落地后同样过全部客户端过滤
- **算法推荐（discovery）**：`ajax/discovery/artworks`（pixiv「みつける」信息流）是免费侧除榜单外唯一的高热内容源——按热度 + 账号兴趣算法挑选，无需 Premium，但**只在携带登录会话时应答**（匿名返回 error 并走重试/空态提示）；条目字段与搜索列表一致（宽高、页数、安全级齐全），一次取 ~180 张入池
- **多图过滤**：所有内容源的条目都自带页数（搜索/详情 = `pageCount`，榜单/touch = `illust_page_count`），多图作品多为漫画或合集——墙只能展示第一页（往往是封面），popup 可一键剔除；页数缺失的条目一律保留，绝不在缺数据时误杀
- **AI 过滤**：pixiv 用 `aiType = 2` 标记 AI 生成作品；popup 可剔除。来源未报告该字段的条目一律保留（同上，缺数据不误杀）
- **收藏数下限**：搜索列表与榜单 JSON 自带收藏数（`bookmarkCount` / `illust_book_count`），popup 可设下限（0 = 不限）；来源未报告收藏数的条目一律保留，否则那些来源会被整体误杀
- **屏蔽画师**：popup 维护画师名列表，按作者名精确匹配剔除（小写不敏感、去首尾空白）——名字是用户在墙上看到的东西，所以列表存的也是名字
；标签源按需分页续取（`api.createTagLoader`）：首批取最新 1–2 页，图池耗尽时自动续取更深页、去重（新上传会使页边界漂移，按 id 去重）、过滤后并入图池，pixiv 翻尽才止——不再是一次加载固定 ~120 张
- **标签热度分层**：热门排序需会员登录，免费侧的热度筛选用 `Nusers入り` 收藏分层关键字——popup 可选 100/500/1000/5000/10000 单档，或「分层混合」：四档页游标同步前进、按 id 去重后并入图池（高档作品少时不会被饿死）
- **标签匹配规则**：pixiv 的 `s_tag` 是模糊匹配（子串 + 翻译别名，短标签会漏进无关作品），`s_tag_full` 是精确匹配但不认识热度关键字。因此：无热度档走 `s_tag_full` 服务端精确；带热度档（含分层混合）走 `s_tag` + 客户端精确过滤（条目自带 tags，归一化比较——小写、下划线视同空格、去首尾空白），翻页耗尽按原始页是否为空判定，整页都是噪声不误杀游标。输入侧 trim（popup 添加与 loader 内部双保险），杜绝带空格的关键词
- **登录会话**：popup 可开关「请求携带 pixiv 登录状态」（默认开）——携带 Cookie 后搜索与榜单按账号自身的浏览设置返回（匿名列表不含 R-18 且限流更紧）；弹窗探测并显示当前登录态，未登录给出登录页入口。登录与否不改变排版纪律：条目宽高仍由列表自带，拼块照旧零裁切
- 边角料治理：任何拼块短边小于 `clamp(视口短边/10, 64px, 110px)` 时，剔除造成它的图并重排（最多剔除 6 张），保证每块都是体面的拼图件
- 展示数量按视口面积自适应（每屏约 `W×H / 85000` 张，8–32 区间夹取）——宁少勿密，每块都是看得过来的大小；窗口 resize 以 150ms 防抖重排
- 图墙是**一视口一屏的拼图墙纵列**：首屏即一块完整拼图；下滑接近墙尾 800px 时追加下一屏（IntersectionObserver），内置榜单耗尽即止，标签源则自动续页（见内容源）——用户下滑，图渐进暴露
- **滚轮翻页**：页面不按像素滚动，一次滚轮手势恰好翻过一整屏拼图，像翻幻灯片。鼠标滚轮一格一个大 delta，触控板是碎 delta 流——累积到 60px（静默 180ms 清零）才算一次翻页意图；滑行期间上锁、**落点即解锁**（scroll 到位检测，兜底 1200ms），一次长甩不会连翻半墙，解码繁忙时也不会吞掉下一个手势。翻到已排版的墙尾时当场追加下一屏，内置榜单耗尽即止（标签源见 §5 内容源的续页）；`prefers-reduced-motion` 下瞬跳不滑行
- 拼块入屏做**涟漪揭示**：从 `translateY(18px) scale(.975)` 浮起归位（420ms），同屏拼块按离左上角的距离以 26ms 阶梯错开（封顶 420ms）——水波从一角荡开，而非整屏砸下；图片本体加载完再从 `scale(1.06)` 沉到 1（Ken Burns 式落座）。被某屏剔除的边角料图回到图池，后面的屏再给一次机会
- 胶囊：`position: fixed`，底部中央，距底 28px；它所在横条是**安全带**——遮挡下方图片点击，避免误触。聚焦升起期间安全带暂时悬空（暗场代行隔离），失焦即复位
- 无其他常驻 UI：设置仍在扩展弹窗里，页面不放开箱即用的设置入口

## 6. 深度层级

- 只有两层常驻：**拼块**与**胶囊**（书签栏属于胶囊所在的安全带，与胶囊同进退，不算新层）。胶囊靠模糊 + 描边 + 阴影浮在拼块上，其余一切与拼块共面。禁止第三层常驻浮层（无角标、无 toast、无页脚）。字幕条、搜索暗场与书签联想面板不算第三层：它们是交互态的临时状态，随指针离开 / 搜索失焦即消失。已收藏拼块的常亮星标也不是角标：它是拼块自身的交互状态标记（同字幕条一块玻璃），且只在交互模式存在。

## 7. Do's / Don'ts

**Do**
- 让插画成为唯一光源；暗角只做气氛
- 用字距和字重建立层级，不用色块
- 键盘可达：`/`、`Ctrl/Cmd+K` 聚焦搜索，`Esc` 清空并失焦，`⇧R` 换一批（重新取源并重排图墙），`Tab` 到开关；书签联想面板展开时 `↑↓` 选行、↵ 打开所选（未选行时 ↵ 仍是网页搜索）

**Don't**
- ❌ 纯黑 `#000000`、紫蓝渐变、霓虹外发光、玻璃拟态滥用（只有胶囊与字幕条两处磨砂）
- ❌ 斜体、渐变文字、居中大标题 hero
- ❌ 给拼块做卡片化装裱（白边、相框、常驻阴影）；圆角与描边只允许是拼块语言（10px 连续圆角 + 毛发描边）
- ❌ 裁切图片适配格子——格子的形状由图决定，不是反过来
- ❌ 用颜色装饰非交互元素
- ❌ 隐藏搜索入口（静止淡出必须保留可辨识的幽灵态）

## 8. 响应式

- 拼块数量与形状全部由排版算法按当前视口实时算出（见 §5），无断点式列数；窗口 resize 150ms 防抖重排
- 胶囊：`min(560px, 62vw)`，窄屏高度降到 44px，模式标签与图标保留（不折叠成图标）；窄屏字幕条内边距收紧
- 无横向滚动；纵向滚动即图墙的分屏渐进（见 §5）；每屏 `100dvh` 量高（禁用 `height: 100vh`）

## 9. Motion 哲学

**气质：慢下来看画，但响应必须瞬时。**

- 缓动：`--ease-out: cubic-bezier(0.23, 1, 0.32, 1)` 用于入场与状态切换；`--ease-spring: cubic-bezier(0.34, 1.4, 0.64, 1)`（轻微过冲）只用于 hover 的迎人一跃。入场/落座 420ms，悬停 220ms，状态切换 180ms，按压反馈 100ms
- 只动 `transform` / `opacity` / `filter`；禁止 `transition: all`、禁止 `ease-in`
- 按压反馈：`:active { transform: translateY(-1px) scale(.99) }`，100ms 内可见
- **签名交互（其一）**：纯看模式下指针静止 2 秒 → 胶囊淡到幽灵态（`opacity .16` + 下移 6px，**不完全消失**），任何指针移动/按键/滚动立即 180ms 复原
- **签名交互（其二）· 聚焦升起**：聚焦搜索（含 `/`、`Ctrl+K`、任意可打印字符触发的聚焦）→ 胶囊从底部安全带升到视口 38% 高度并加宽加高（340ms `--ease-out`），暖色暗场同步压下图墙（340ms 淡入），微标签与提示行延迟 60/80ms 跟进——仪式感在上升的一程；失焦/Esc 落回底部（240ms，归程从简），暗场 240ms 淡出。`prefers-reduced-motion` 下全部瞬切
- **滚轮翻页**：一手势一屏（见 §5）；滑行用浏览器原生 `scrollTo({behavior:'smooth'})`，reduced-motion 瞬跳
- 拼块入屏涟漪：上浮 18px + `scale(.975→1)` + 淡入 420ms，逐块触发（IntersectionObserver）并按离左上角的距离 26ms 阶梯错开；图片落座 `scale(1.06→1)` 与之叠加。悬停规则一律 `transition-delay: 0`——涟漪的残余延迟永远不许拖住指针
- 收藏星标：随字幕条同拍浮入（160ms，自 `translateY(-4px)`），按压 `scale(.92)` 100ms；点亮/熄灭只做色彩与填充切换，不跳位
- 书签联想面板：行选中高亮 120ms；面板随升起态同生共灭，不单独做入场动画（它出现的时刻胶囊已经在动）
- 纯看 ↔ 交互切换：开关滑块 180ms；图墙不做整屏过渡，只有 hover 能力开关
- 书签栏：幽灵态与胶囊同拍（180ms + 下移 6px）；条项 hover 160ms 亮起；不做入场动画（它随页面一起在）
- popup：状态切换 140–200ms（开关、勾选、分段、hover）；登录呼吸点 2.6s 透明度脉动，reduced-motion 下静止
- `prefers-reduced-motion: reduce`：去掉位移、淡入与阶梯延迟，只保留即时状态变化
- hover 相关效果一律包在 `@media (hover: hover) and (pointer: fine)`

## 10. DNA 与工艺密度（可溯源）

**偷了什么（craft-loop 强制）**
1. 偷 Raycast：暖调近黑画布（非纯黑）+ `rgba(255,255,255,.06)` 描边策略
2. 偷 Raycast：多层 inset-highlight 阴影栈（见 §4 胶囊）
3. 偷 Raycast：暗色界面用**正字距 +0.2～0.4px**
4. 偷 Apple：全页**单一重音色**且只服务交互；`2px` 品牌焦点环
5. 偷 SpaceX：10px 大写 + 1px 字距的微标签，以及图像上方的暗渐变遮罩保证文字可读

**工艺密度（≥5，逐项可指认）**
1. 氛围层：整页暖色径向暗角（`<6%`）——§1
2. `::selection` 定制：文字选中用 Accent 底 + 深字
3. 品牌 focus 环：`2px` Accent + 2px offset，替换浏览器默认蓝
4. 多层染色阴影：胶囊三层阴影栈——§4
5. 自定义滚动条：细窄、半透明白 thumb，与暗底同调
6. 编辑细节：模式标签用 10.5px 正字距小字，随状态换文案
7. 签名交互：纯看模式的静止→幽灵态——§9

## 11. 状态完整性

- 图墙加载中：图片随拼块涟漪淡入落座（420ms，见 §9），容器保持暗底不闪白
- 图墙空/接口失败：先静默重试两次（2.5s、5s）——浏览器/扩展刚重启时代理栈尚未就绪，整批请求会一起超时，一次 F5 能解决的事不该直接摊牌；重试耗尽后才显示居中一行次文字说明 + 「打开 pixiv」文字链（不假装有内容）
- 搜索失败：输入框下方 180ms 淡出一条次文字提示，说明下一步（重试 / 直接访问搜索引擎）
- 纯看模式：开关标签显示「纯看」、`cursor: default`、图片完全无 hover 反馈；不额外加提示条（开关本身就是状态说明）
